<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.airlift</groupId>
        <artifactId>airbase</artifactId>
        <version>395</version>
    </parent>

    <groupId>dev.jaaj.trino</groupId>
    <artifactId>trino-search</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>trino-plugin</packaging>
    <description>Trino - Search functions</description>

    <properties>
        <air.java.version>25.0.1</air.java.version>
        <project.build.targetJdk>25</project.build.targetJdk>
        <dep.trino.version>483</dep.trino.version>
        <dep.airlift.version>439</dep.airlift.version>

        <!--
          trino-testing pulls trino-testing-containers transitively, whose testcontainers
          and docker-java dependencies carry netty and docker-java-transport jars whose
          versions the io.airlift bom does not align with trino's own root pom. Trino's
          root pom works around this with hand-picked duplicate-finder exceptions; a
          plugin skeleton has no equivalent inventory to maintain, so the check is
          disabled instead of chasing every future transitive shift.
        -->
        <air.check.skip-duplicate-finder>true</air.check.skip-duplicate-finder>

        <!--
          trino-main's BlockEncodingSimdSupport touches jdk.incubator.vector, an incubator
          module that is not on the module path unless requested explicitly. Trino's own
          root pom adds the same flag to its test JVM arguments.
        -->
        <air.test.jvm.additional-arguments>--add-modules=jdk.incubator.vector</air.test.jvm.additional-arguments>

        <!--
          Airbase's default PMD ruleset references the pre-7.x resource path
          /rulesets/java/basic.xml, which does not exist in the PMD 7.26.0 that this
          build resolves; the pmd goal fails before analyzing any source. Trino's own
          root pom disables this same check (air.check.skip-pmd=true) for the same
          reason.
        -->
        <air.check.skip-pmd>true</air.check.skip-pmd>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.airlift</groupId>
                <artifactId>bom</artifactId>
                <version>${dep.airlift.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>slice</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-spi</artifactId>
            <version>${dep.trino.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-main</artifactId>
            <version>${dep.trino.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-testing</artifactId>
            <version>${dep.trino.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>ca.vanzyl.provisio.maven.plugins</groupId>
                    <artifactId>provisio-maven-plugin</artifactId>
                    <version>1.1.3</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>io.trino</groupId>
                <artifactId>trino-maven-plugin</artifactId>
                <version>20</version>
                <extensions>true</extensions>
            </plugin>
        </plugins>
    </build>
</project>
